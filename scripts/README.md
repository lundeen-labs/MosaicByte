# Scripts

Maintenance and automation scripts for the Mosaic Byte project.

## Available Scripts

- `generate-sitemap.mjs`: Post-build script that crawls the `src/routes` and `content/case-studies` to generate a production `sitemap.xml` in the `public` and `dist` directories.
